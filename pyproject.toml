[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "qtapp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "PySide6>=6.7",
]

[project.scripts]
qtapp = "qtapp.app:main"

[tool.setuptools.packages.find]
where = ["src"]
